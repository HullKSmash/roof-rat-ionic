export class Route {
    
    constructor(id: number, name: string, length: number, image: string, description: string) {}

    id: number;
    name: string;
    length: number;
    image: string;
    description: string;
}